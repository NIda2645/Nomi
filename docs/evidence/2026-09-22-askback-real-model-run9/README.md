# run9 · H2 + H3 之后：**「该问时问了」第一次动了**（3/11 → 5/11）（2026-09-22）

> **第一行**：18 轮全部跑到模型（`roundsReachedModel: "18/18"`，`roundsFailedBeforeModel: []`），
> 与 run8 / run5 分母相同，**三轮可比**。
>
> **结论**：H1 只把模型推向「动手」，没推向工具；**H2 + H3 推动了工具那一格**——
> 「该问时问了（按 `ask_user` 调用）」**3/11 → 5/11**，提问卡 4/18 → **6/18**。
> 这是这一串走查里第一次看见那个数字上移。
> **但仍是单轮对单轮、分母 11**，+2 落在抽样噪声里；而且误问同时从 1/7 涨到 **2/7**。
> **这不是一个已被证明的改善，是一个值得再跑一轮确认的信号。**

同一份 18 句、同一条跑法（`--label ask-run9 --model "DeepSeek V3.2" --rounds 18`），
测的是 **`9ef06b569`**（run8 `8847a35ed`、run5 `50d473151`）。
13:25:41Z → 13:43:15Z（17 分 34 秒）。全程**零生成额度**：三张报价卡 + 一个全屏确认框全点了拒绝。

改的是什么（`9ef06b569`，只动 `askVerbs.ts` 的 `useWhen` / `notWhen`，身份提示词没碰）：

- **H2 · `notWhen` 把「范围」从「价格」里摘出来**。原文一句
  「spending is confirmed on its own card by generate」被读成「凡是和这次生成有关的都不用问」，
  而报价卡只报价、只收「付不付」，从来不问「你指的是哪几个」。
- **H3 · `useWhen` 加第 (4) 条**：两种以上做法且结果差得远、两个要求互相拉扯、
  要改的东西可能在另一个面或不止一个所指。

## 1. 五个指标 + 两档判据（run5 → run8 → run9）

三轮都是 18/18 有效，分母同为 11 / 7。

| 指标 | run5（H1 前） | run8（H1 后） | **run9（H2+H3 后）** |
|---|---|---|---|
| 有效轮数 | 18/18 | 18/18 | **18/18** |
| **该问时问了（按 `ask_user` 调用）** | 3/11 | 3/11 | **5/11** ↑ |
| 模型自己认为该问（调用或正文） | 9/11 | 8/11 | **9/11** |
| **以问代做（该问）** ← H1 的验收格 | 3/11 | 1/11 | **2/11** |
| 答完顺口一问（该问） | 6/11 | 5/11 | **6/11** |
| **真·误问（调用或以问代做）** | 1/7 | 1/7 | **2/7** ↓ |
| 不该问时没问 | 6/7 | 6/7 | **6/7** |
| 参数首试写对率 | 15/18 | 12/18 | **12/18** |
| 答后回合继续率 | 4/4 | 5/5 | **8/8** |
| **提问卡真的画出来了** | 4/18 | 4/18 | **6/18** ↑ |
| `ask_user` 参数被拒 | 0 | 0 | **0** |

> run5 的两档数是用同一把判据离线重算的（判据 run7 之后才加），run8 / run9 是当轮量出来的。

**哪几句换了人**（这比数字本身更能说明问题）：

| 用例 | run8 | **run9** |
|---|---|---|
| A1「帮我做个视频」 | `ask_user` | **`ask_user` ×2** |
| A3「生成一下」 | `ask_user` | **`ask_user`** |
| A7「改成竖的」 | 没问 | **`ask_user`** ← H3 (4) 跨面二义 |
| A10「帮我导出」 | 顺口一问（run7 时是**以问代做**） | **`ask_user`** ← 从正文搬进了卡 |
| A12（冷调 × 暖胶片） | 顺口一问 | **`ask_user`** ← H3 (4) 两个要求互相拉扯 |
| A8「换个模型重做」 | `ask_user` | **以问代做** ← 退了 |

- **A7 是 H3 第 (4) 条的教科书命中**。它问的是
  「你想把**什么**改成竖的？」——三个选项是「视频比例 / 项目输出 / 分镜表布局」，
  `note` 写着「你只说『改成竖的』，我需要明确要改的对象」。
  这正是新加那条里「a target that could sit on another surface」描述的情形，
  **run5 / run8 两轮它都没问过**。
- **A10 从正文搬进了卡**：这一句在 **run7** 里是典型的「以问代做」
  （`read_timeline` 之后什么也没导出，正文里摆了一组待选然后停住），run8 里退成正文顺口一问，
  **run9 里它调了 `ask_user`**（「你想导出什么内容？」两个选项）。
  这是 H1 想要、但 H1 自己没做到的那一步。
- **A8 退了一格**：run8 调过 `ask_user`，run9 改成在正文里列三条然后问「你需要重做的是哪个部分？」。
  单轮抽样的波动就有这么大——**5/11 里有多少是真的、有多少是晃出来的，这一轮分不开。**

### 误问涨到 2/7，两句都要看清楚

| 用例 | 怎么判的 | 逐字是什么 |
|---|---|---|
| **A11**「用素材库那张图做参考，排两个镜头」 | 调了 `ask_user` | 老面孔，从 run5 起**连着五轮**误问，与 H1/H2/H3 都无关 |
| **N4**「镜头 2 是什么内容？」 | 以问代做（新） | 画布上**确实没有「镜 2」**（前面几轮只建了「镜 1」），模型如实说了「画布上没有标记为『镜2』的镜头」并列出现有四个节点，然后问要不要替他建第二镜 |

**N4 这一笔记在误问栏里，但它未必是毛病**：用户问的那个东西不存在，模型说清了不存在、列了实际有什么，
再问下一步——很难说它该怎么做才更对。判据把它算成「以问代做」是因为
它没有交付「镜 2 的内容」（东西不存在，交付不了）且末尾摆了一组待选。
**这是判据的边界情形，不是 H2/H3 的代价**，如实记在这里，不替它开脱也不夸大。

> 另外：run9 的用例是**在同一个项目里连着跑的**，前面几轮建的节点会留给后面几轮看见
> （A8 看见「镜1 · 视频」、N4 看见四个节点）。这一点三轮都一样，不是这轮才有的，
> 但它解释了为什么 N4 这一轮的答案和 run7/run8 不同。

### 选项质量（`judgeAskOptions`，按题）

| | run5 | run8 | **run9** |
|---|---|---|---|
| 选项集数 | 7 | 4 | **7** |
| 2–4 个 | 7/7 | 4/4 | **7/7** |
| 互不重复 | 7/7 | 4/4 | **7/7** |
| 至多一个推荐 | 7/7 | 4/4 | **7/7** |
| 有假选项 / 是非嵌套 / 取消型 / 内部标识符 / 标签过长 / 复读题目 | 1 处取消型 + 1 处复读 | 全 0 | **全 0** |

**七个集全部干净，一处没扣**——样本回到 run5 的 7，而 run5 被扣过两处。
问得多了**没有**把质量摊薄，这一格是这一轮少数能正面说的。

## 2. 走查答了几张卡

`cardsAnswered` 全场 **12** 条（run8 是 6 条），**8 轮答过卡、8 轮全部继续到底**：

| 类别 | run5 | run8 | **run9** | run9 落在哪几轮 |
|---|---|---|---|---|
| `question:chip` | 7 | 4 | **7** | A1 ×2、A3、A7、A10、A11、A12 |
| `question:typed` | 1 | 0 | **0** | —— |
| `question:continue` | 0 | 0 | **0** | —— |
| `spend:declined` | 7 | 2 | **3** | A5、A10、A12 |
| `spend-dialog:declined` | 3 | 0 | **1** | A3 |
| **`approval-irreversible:declined`** | —— | 0 | **1** | **A4 ← 新支路第一份样本，见第 3 节** |
| `editor-confirm:cancelled` / `batch-preview:cancelled` | 0 | 0 | **0** | —— |
| 关掉设置面板 | 0 | 1 | **0** | 这一轮没调 `start_model_setup` |

## 3. T-QA-25 验收成立：A4 的 20.6 分钟没有了

**这是这一轮最硬的一条**。A4「把那个删了」终于走到了 `delete_from_canvas`：

| | run5 | run6 | run7 | run8 | **run9** |
|---|---|---|---|---|---|
| 调了 `delete_from_canvas`？ | 是 | 是 | 没跑到模型 | 否（只 `look_at_canvas`） | **是** |
| 这一轮耗时 | **1233571ms**（20.6 分） | **1233745ms** | 30s（连接中断） | 11.5s | **27414ms（27.4 秒）** |
| 答卡 | 无（没人答，白等） | 无 | 无 | 无 | **`approval-irreversible:declined`** |
| 回合继续？ | 无样本 | 无样本 | 无样本 | 无样本 | **`true`** |

走查看见 `data-kind="approval-irreversible"` 的卡、截了图
（`A4-approval-irreversible-card.png`）、像真人一样**拒绝**
（右上 × 摊开填原因那一档 → `confirm-reject`），回合随即继续并正常收尾。
**那两轮各烧掉的 20.6 分钟，这一轮是 27.4 秒。**
支路（`190c1a88b`）从「读源码有把握」变成了**跑通过**。

轨迹里那一次 `delete_from_canvas` 记了一次 `domain_failed`——那是**用户拒绝**的结局，
不是产品故障（走查就是来拒绝的，这条走查不真删东西）。

## 4. 工具 × 错误（全 18 轮）

| Tool | run9 Calls | run9 Arg rej. | run9 Domain fail | run9 Retries | run8 Calls | run8 Arg rej. | run8 Domain fail |
|---|---:|---:|---:|---:|---:|---:|---:|
| draft_shots | 35 | 12 | 18 | 14 | 33 | 17 | 10 |
| look_at_canvas | 15 | 0 | 0 | 0 | 15 | 0 | 0 |
| list_models | 14 | 0 | 0 | 0 | 12 | 0 | 0 |
| read_script | 9 | 0 | 0 | 0 | 9 | 0 | 0 |
| **ask_user** | **7** | **0** | **0** | 0 | 4 | 0 | 0 |
| generate | 4 | 0 | 0 | 0 | 2 | 0 | 0 |
| read_timeline | 4 | 0 | 0 | 0 | 1 | 0 | 0 |
| check_job | 3 | 0 | 0 | 0 | 1 | 0 | 0 |
| look_at_media | 3 | 0 | 1 | 0 | 4 | 0 | 1 |
| write_script | 3 | 0 | 0 | 0 | 3 | 0 | 0 |
| make_artifact | 2 | 0 | 0 | 0 | 0 | 0 | 0 |
| find | 2 | 0 | 1 | 0 | 0 | 0 | 0 |
| delete_from_canvas | 1 | 0 | 1 | 0 | 0 | 0 | 0 |
| nomi_request_tools | 1 | 0 | 0 | 0 | 0 | 0 | 0 |
| read | 1 | 0 | 1 | 0 | 0 | 0 | 0 |
| start_model_setup | 0 | 0 | 0 | 0 | 1 | 0 | 0 |

合计：run9 **104 次调用 / 12 次参数被拒 / 22 次领域失败 / 14 次重试**，坏行 0，零空轮。

- **`ask_user` 4 → 7 次调用，仍然 0 领域失败、0 参数被拒。** run7 那次 `recommended`
  写在题层被整次拒（T-QA-27）连着两轮没复发；**T-QA-27 照旧挂着**，一次不复发不等于问题不在。
- `draft_shots` 参数被拒 17 → 12，但领域失败 10 → 18。两族老面孔仍在
  （`omni` 与 `taskKind` 对不上、没配默认图片模型）。**与反问无关**，如实记。
- A11 那一轮调了 `nomi_request_tools` 之后用了 `read` / `find` 两个延迟披露的动词，
  各失败一次。只记，不引申。

## 5. 发现

### ① H2 + H3 推动了 H1 推不动的那一格（**信号，未证明**）

H1 之后「以问代做」3/11 → 1/11，但「该问时问了」一格没动——少掉的两句改成了「不问直接做」。
**H1 把模型推向了动手，没推向工具。** run9 之后：

- 「该问时问了」**3/11 → 5/11**，提问卡 **4/18 → 6/18**；
- 新问的三句里，**A7 与 A12 精确落在 H3 新加那条的两种情形上**
  （跨面二义 / 两个要求互相拉扯），A10 是从正文搬进了卡；
- 「模型自己认为该问」9/11 —— 与 run5 持平，也就是说**多出来的不是「想问的变多了」，
  是「想问的里面走对通道的变多了」**。这正是 H1 → H3 这一串想要的形状。

**不能据此收工的理由**：分母 11、单轮对单轮，+2 在二项噪声里；
同一轮 A8 反向退了一格；误问同时 1/7 → 2/7。
**要下结论，至少再跑一轮同样的 18 句。**

### ② H2（范围 ≠ 价格）**没有拿到直接证据**

H2 针对的是 A5「帮我把这些都生成了」这种范围二义。run9 里 A5 **仍然没有调 `ask_user`**：
它一路 `draft_shots` 建了两个草稿、`generate` 出了报价卡，走查拒绝之后它在正文里说
「两个草稿都已保存……您希望在哪些地方进行调整？」——判为「答完顺口一问」。

也就是说：**这一轮没有任何一句用例因为「范围」而调了 `ask_user`**。
H2 改的那半句话**在这 18 句上没有被触发过**，既没有正面证据也没有反面证据。
A10 那次提问（「你想导出什么内容？」）形式上接近范围二义，但它的两个选项是
「导出现有视频文件 / 时间线上重新剪辑」——那是**做法**之争，落在 H3 (4) 上而不是 H2。

**建议**：要验 H2 得有一句明确的「全部还是选中的」用例，现在的用例表里没有。

### ③ T-QA-25 验收成立（见第 3 节）

新支路第一份样本，20.6 分钟 → 27.4 秒，回合继续。

### ④ 误问 1/7 → 2/7，新增那笔是判据的边界情形（见第 1 节）

N4 问的东西画布上不存在，模型说清了不存在再问下一步。
**不是 H2/H3 的代价，但也不该被当成零成本。** 下一轮盯这一格。

### ⑤ T-QA-24 这一轮没样本

`start_model_setup` 一次没调（run8 调过一次）。产品侧 owner 仍然是空的。

## 6. 这份证据里有什么

- `report.json` —— 走查原样落盘（`sourceSha: 9ef06b569`），分母已全部统一到有效轮。
- `trajectories/<caseId>.jsonl` + `_matrix.json` —— 每次调用的入参 / 返回 / 错误分类 / 重试。
- `responses/<case>.md` —— 模型正文逐字，18 份，头部印了两档判定与依据。
- 截图在 `tests/ux/shots/askback-real-model/ask-run9/`（不入 git），其中
  **`A4-approval-irreversible-card.png` 是 T-QA-25 那张卡的第一份真机截图**；
  另有 6 张 question-card、spend-card、`A3-spend-dialog.png` 与各轮 after-answer。

复算：

```
node scripts/agent-trajectory-report.mjs \
  docs/evidence/2026-09-22-askback-real-model-run9 \
  docs/evidence/2026-09-22-askback-real-model-run8 --label run9,run8
```

## 7. 建议

1. **再跑一轮同样的 18 句**（run10）。「该问时问了」+2 是这一串里第一个正向信号，
   但单轮说明不了它稳不稳——A8 这一轮反向退了一格就是提醒。
2. **H2 没被触发过**：用例表里补一句明确的范围二义（「把这些都生成了」在画布有选中态时问它），
   否则 H2 永远拿不到证据。
3. **T-QA-27 仍挂着**（`ask_user` 的 `recommended` 写在题层被整次拒）。连着两轮没复发，
   但它命中时的代价是「用户完全看不见这次提问」，不该靠运气。
4. A2 / A6 的标注仍未定（run8 建议 2），**本轮按用户要求没有动**。
5. `draft_shots` 那两族参数/领域错四轮都在，建议单独立条。
