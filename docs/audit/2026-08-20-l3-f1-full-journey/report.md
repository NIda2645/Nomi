# L3-F1 真额度全旅程验收报告（2026-08-20）

> **这是整套三层验收体系（L1 单测 / L2 旅程 / L3 真额度）造出来要跑的那一跑。**
> 对象：用户提的三条要求——① 给额度但别花太多；② **必须由真实用户去体验**，把任务发给桌面端软件真实地做出来；
> ③ 最终质量和整个流程必须确实符合预期。
>
> 执行：打包二进制（Authority=Nomi Local Codesign）+ 用户真实 apimart 目录 + 隔离 projects/capability。
> 驱动 `scripts/l3/l3-f1-full-journey.mjs`。**13 件产物全部由我逐张 Read 亲眼看过（含每条视频的尾帧）。**

## 跑了什么

短剧《最后一班》：便利店夜班收银员发现每晚 2:17 都有同一个身影经过，第三夜她跟了出去。
三段式（钩子 → 升级 → 反转），6 镜 + 1 张角色定妆卡。

**实花：图 4 张 / 上界 34 · 视频 6 段 / 上界 8 · 全程 17 分钟。** 远低于用户给的预算。

## 判据对账

| # | 判据 | 结果 |
|---|---|---|
| ① 打扰预算 ≤4 次 | **实测 1 次** | ✅ 大幅达标。且那一次的问法很好：说清了模型、prompt 摘要、**「重试也算这次额度」**、会话信任范围（20 次后再确认） |
| ④ 交付完整 | 7/7 成功，红标 1 处如实呈现 | ✅ 结构齐（过检结论/红标/深链） |
| ⑦ 同角色跨镜 identity ≥4 | **4.50**（分母 2 镜，另 1 镜判不了） | ✅ 达标，但**样本太小，不足以宣布这条通过** |
| ⑧ 人眼终闸：未标红的崩坏 = 0 | 无解剖崩坏 / 无张冠李戴 / 无伪影 | ✅ **但发现了反向问题**——见下「误报」 |

## 人眼逐张核对（我真看了首帧 + 尾帧）

| 镜 | 机器判（只看首帧） | 我看首帧 | 我看**尾帧** | 结论 |
|---|---|---|---|---|
| 定妆卡 | id 3 / comp 5 | 短发圆脸、眉上痣、单眼皮、深蓝工装、浅灰背景——**逐项对得上圣经** | — | ✅ 好锚 |
| #1 挂钟 | id 0 / comp 5 | ⚠️ **场景漂移**：维多利亚书房座钟，不是便利店挂钟 | 玻璃面上人形倒影**真的浮现** | 场景错，戏剧点对 |
| #2 她抬头 | id 4 / comp 4 | ✅ 便利店 + 冷白光 + 人对 | 同一人，抬头动作完成，**无中途变脸** | ✅ 最好的一批 |
| #3 玻璃门 | id 0 / comp 5 | ⚠️ **场景漂移**：书架走廊 | **湿脚印真的浮现**，画面很有戏 | 场景错，戏剧点对 |
| #4 她推门 | id 0 / comp 5 | ✅ 背影 + 深蓝工装 + 短发 | 跨出门槛、夜风吹动衣角、城市夜景 | ✅ **id=0 判得对**：背影镜本就没有可比对的脸 |
| #5 空街反转 | id 0 / comp **1 · 红标** | 街道/积水/昏黄路灯都对，人影未现 | **人影出现了**——路灯下站着一个背影 | ❌ **红标是误报**，见下 |
| #6 结尾特写 | id **5** / comp 3 | ✅ **同一个人**：痣的位置、脸型、短发、单眼皮全对上 | 惊愕情绪弧走完，身份稳 | ✅ 身份满分；场景（暖黄路灯）错 |

**六镜里每一镜的戏剧点都落在尾帧，而判分器一帧都没看见。**

## 三个结构性缺陷（都是真额度换回来的，不是预设用例）

### A. I2V 两跳在主力视频模型上**从来没触发过** —— W2 的招牌功能是死的

`shouldUseTwoHop` 用**手写正则**猜「这个模型有没有首帧槽」：`/first_frame|firstframe|start_image|image_url$|^image$/i`。
Seedance 的 i2v body 用的是 `image_urls`（复数）——`image_url$` 要求以 `image_url` 结尾，**匹配不上**。

实测（不是读代码推断）：Seedance 2.0 derive 出的 10 个参数键
`audio_urls, duration, generate_audio, image_urls, image_with_roles, model, resolution, seed, size, video_urls`
**没有一个命中**。Kling 的 `first_frame_image` 能命中，主力的 Seedance 命中不了。

**后果被这一轮量化出来了**：两跳不跑 → `ffDesc`（分镜精心写的静态场景描述）一个字都用不上 →
**4/6 镜场景漂移**。#2/#6 身份满分（锚图直接进了 i2v 参考通道）但场景错（描述没送达）——
身份和场景在这里是两条独立的通路，只有一条通着。

**更深一层**：Seedance 的首尾帧槽其实是 `image_with_roles: [{url, role:'first_frame'|'last_frame'}]`，
而我们的投影发的是 `first_frame_url`。**即使判据修好让两跳触发，那张首帧图也到不了 Seedance。**

**根因不是「正则漏了一个 s」**——是拿手写模式去猜各家键名，下一家换个叫法照样漏。
目录里本来就有 derive 好的参考承载力（`bodyReferenceSupport` / `referenceModeForIntent`，`list_models` 用的同一份源）。
判据该问那份，不该自己再写一套。

**连带**：上一轮 L3-W2 报告里「② I2V 两跳真跑」的结论**是错的**——同一个模型，同样没触发；
我把判分抽出来的帧当成了两跳的产出。已在本报告更正。

### B. 审片环对视频**只看第 1 帧** —— 造成误报 + 对头号失败模式失明

`extractFrame` 只问 `which:'first'`。而「逐渐显出 / 由暗转亮 / 缓缓推近」是短剧最常见的镜头语言。

三层后果，本轮全部实地撞上：

1. **误报**（#5）：那一镜的设计就是「远处路灯下**逐渐**显出背影」。判分器在 t=0 看，什么都没有 →
   构图 1 档 → 重滚一次 → 仍 1 档 → 红标。**而尾帧里人影明明出现了。** 红标是信任的基石，
   误报和漏报一样毁它——用户被告知「你这镜坏了」，其实好得很。
2. **白烧额度**：那次重滚是为一个不存在的问题付的钱。
3. **对「动到一半人就变了」完全失明**：这是视频生成的头号失败模式，而判分器结构上看不到。
   本轮两条带锚镜没有漂移——**但那是我手动抽尾帧验的，系统不知道。**

能力本来就在：`extractVideoFrame` 支持 `first | last | 秒数`（文件头注释白纸黑字写着），只是没用上。

### C. 连贯轴从来没评过（本轮已修，待验）

全 6 镜 `continuity=—`。`previousShotPrompt` 从没被传过——headless 每次 `nomi_generate` 都是独立调用。
**审片环对外说三轴，实际只跑了两轴**，而「接不接得上」恰是短剧最容易崩的那轴。

上一镜可从 `shotIndex` derive，不是拿不到。已补 `previousShotPromptFor`（判不出就返回空，
不拿错的镜去比——错参照物比不比更糟，会凭空判出断裂再触发白烧的重滚）。**本轮跑的是修之前的包，待复验。**

## 一个小但真实的交付问题

角色定妆卡的胸前工牌上，模型自己脑补出了 **7-Eleven 的真实商标**，并在 #2/#6 里一路带下去（3 处）。
不是崩坏，但对要交付的成片是真问题（商标）。锚提示词里没提任何品牌——是「便利店工装」触发的模型先验。

## 已修（本轮）

| 修复 | 针对 |
|---|---|
| `composeShotPrompt` | 没走两跳时把首/尾帧描述折进提示词，场景信息不再蒸发（走两跳时原样不动：静态信息已由真图承载，再用文字复述会和图打架） |
| 判分对齐 `effectivePrompt` | 判分器改为对着**我们真正发出去的那份提示词**判。否则上游丢了信息，判分环结构上就抓不到（#1/#3 拿 5 分正是这个原因） |
| `previousShotPromptFor` | 接上连贯轴的参照物 |

## 待修（已定位根因，未动手）

1. **两跳判据改用目录 derive**（缺陷 A）+ **Seedance 首尾帧槽的投影**（`image_with_roles` 角色数组）。
2. **视频判分改看首尾两帧**（缺陷 B）——顺带让 identity 轴能查中途漂移。
3. 判据⑦ 复验：本轮分母只有 2 镜，**不足以宣布通过**。

## 结论（诚实版）

**流程跑通了，质量有真实短板，短板的根因全部定位到了。**

- 立项 → 圣经 → 定妆 → 拆镜 → 逐镜生成 → 审片 → 重滚 → 红标 → 交付，**整条闭环真的走完了**，
  17 分钟、只打扰用户 1 次、花费远低于预算。
- 跨镜身份**真的成立**：#6 的痣、脸型、短发和定妆卡对得上，#2 同理，尾帧都没漂。
- 审片环**真的在干活**：#5 被抓出来、重滚、红标不藏——只是这一次抓错了（首帧盲区）。
- **但「初稿质量尽可能好」这个目标还没达到**：4/6 镜场景漂移，根因是招牌功能两跳在主力模型上是死的。

按用户要求③「质量和流程必须确实符合预期」——**流程符合，质量不符合**。缺陷 A/B 修完并复验之前，
这条不算过。不粉饰。
