# 简化 A＋完整 T7：独立 PR 审查提示词

将下面内容连同最终 PR 链接发给审查 AI。审查仅只读，不修改、批准或合并 PR。

---

请独立审查这个 PR：`<交付时填入 PR URL>`。不要把作者报告或测试数量当结论；核对实际源码、差异、测试质量及截图。先读目标仓库 AGENTS.md、其引用的工程规则，以及下列范围依据：

- `docs/audit/evidence/core-a-20260920/scope/Nomi_plan_A_core_fixes_2026-09-19.md`
- `docs/audit/evidence/core-a-20260920/scope/B-T7-excerpt.md`（指定 B 的完整 T7；其余章节不是本次范围）
- `docs/audit/evidence/core-a-20260920/scope/Nomi_simplified_A_plus_T7_original_storyboard_plan_2026-09-20.md`
- 仓库 `docs/plan/2026-09-20-core-a-restart.md` 为实施范围依据；`docs/audit/2026-09-20-core-a-full-review.md` 为历史发现及结构复核依据，T7 和 remaining-acceptance 保留历史记录。唯一现行结果见 `docs/audit/2026-09-20-core-a-current-acceptance.md`，勿把旧快照的运行中或待验措辞当现状。

先获取真实 PR base/head SHA 并计算 merge-base，审完整分支 diff，检查所有新增文件；不要把主线前进造成的两点差异当成回滚。若缺少任务书或证据，明确缺失项，不猜范围。

本次只做简化 A＋完整 T7。必须保留原 StoryboardPlanEditor → storyboardRowActions → canvas runner，接好新建、Agent 交互、目标、保存恢复与节点绑定；不得以适配为名新增替代页面、第二份可编辑正本、第二条执行或付款链，不自动展开 T3。原界面不要被 Agent 顶部新增信息挤压。PR #808 左栏收起让位及手动偏好是当前保留行为；左右栏同时展开的整体空间设计已由用户明确延后，见唯一 TODO T-DS-01/A-2。不要擅自要求本 PR 重画布局。

逐项检查：

1. 工程结构：每个新增抽象是否必要，是否复用原 owner，是否重复状态/恢复路径/校验词表；是否存在只修症状、fallback 或靠弱化测试/预算/基线过关。检查根因合同的门表是否覆盖实际调用链，不能只看文档存在。
2. 分镜完整旅程：两文稿各两方案；prompt、模型参数、参考槽、首帧完整保存重开；Agent 等待期间切换、目标修改或删除；迟到返回不覆盖新目标。区分捕获原目标继续处理和错误追随当前焦点，不要求所有切页都取消。
3. 原生成与绑定：按需建节点、重复落画布幂等、覆写/结果/历史保留；原单镜、批量、首帧依赖波次；审批后作者变化或节点删除不得使用旧快照付费，批量每个镜头/每波都应传递校验。×3 若属于原基线入口缺口，核实归属并准确说明，不用 runner 单测冒充实际 UI 通过，也不借机扩大产品范围。
4. 付款与身份：逐镜/批次/全部选择准确；确认 A 不丢 B 未确认草稿；关闭不删节点或结果、不提交媒体；重新申请有新批准且草稿保留。校验 quote/CAS/模型/金额/素材版本、重复确认/回放、授权等待期间项目替换、任务 domain+ID；已授权后后台继续投递原项目的行为应保留。未知价格不冒充零价，测试不得伪造真实目录价格。
5. 完整 T7：单选/多选、历史切换、异常拖动结束、blur/unmount、lazy 失败、零尺寸恢复、边缘与底栏定位；画布和付款卡两宿主。要求实际能输入/点击、写入正确对象，不能只断言 DOM 存在。
6. A 其余部分：安全错误和诊断脱敏、技能附件、原消息重发、Stop/Continue、实际 pi 压缩历史、旧消息/旧授权隔离、删除/Undo/输入焦点，不能只审分镜页。
7. 证据真实性：分别核对受控测试、真实 Electron＋loopback、真实供应商付费、候选安装包。真实任务 ID、可解码产物、供应商返回费用和热/冷重开应互相对应；截图必须来自被测构建并覆盖 zh/en、实际改动区域。确认最终源码 SHA/tree、包和测试记录关系。Windows 未验证不得写成通过。审查历史失败是否真的归因，不能拿后来重跑绿代替根因证明。

报告先列阻塞发现，按 P0/P1/P2 排序。每项给出文件行号、可复现条件、用户后果、原有还是本轮引入、最小合理修复方向及缺失测试。区分已证缺陷、待验证怀疑与建议，不把臆造的 await/不真实 mock 当生产竞态。再列覆盖范围与仍未验证项，最后给出“可进入用户合入决策”或“应修复后复审”。如果没有发现，也明确剩余风险。

不要 approve、merge、squash、close PR 或开启 auto-merge。只给审查报告，由用户决定下一步。
