{
  "//": "测试文件（src/ + electron/ + evals/）的类型检查配置。由 `pnpm run check:test-types` 使用，",
  "//2": "不进 `pnpm typecheck`——那两份配置（tsconfig.app.json / electron/tsconfig.json）都 exclude 了测试，",
  "//3": "而 vitest 用 esbuild 转译，只删类型标注不核对。没有这份配置，测试文件的类型就没有任何工具在看。",
  "//4": "为什么另起一份而不是删那两处 exclude：electron 的测试会 import src/ 下的东西（违反它 rootDir: \".\"），",
  "//5": "且用了 ESM 顶层 await（与它 module: CommonJS 冲突）——直接删 exclude 会炸出 112 个纯配置形状错，全是噪音。",
  "//6": "target/module 按**测试的真实运行环境**取：vitest 在 Node 里跑测试（不是浏览器、不是 CommonJS 主进程），",
  "//7": "所以 ES2022 + ESM 才是诚实的；这也是 Object.hasOwn 之类写法在测试里合法的原因。",
  "//8": "scripts/**/*.ts 也归这份管：它们由 tsx 直接跑、同样没有别的工具看类型。2026-09-17 生产端给 LocalSpeechProgress",
  "//9": "加了 starting 阶段，scripts/local-speech-live-check.ts 的二元三目原样把它当 transcribing 读，脚本一条素材都跑不完——",
  "//10": "tsc 早就能报 TS2339，只是没人让它看这个目录。allowImportingTsExtensions 对应 tsx 的运行时解析（noEmit 下合法）。",
  "//11": "allowJs：门岗脚本按「.ts 门岗 + .mjs 纯逻辑库」分层（node --test 直接跑那个库，所以它必须是能被 node 跑的 JS）。",
  "//12": "没有 allowJs，tsc 对每一条这样的 import 报 TS7016 并把整个库当 any——被检查的那一面正好是最该看的那一面。",
  "//13": "打开它 tsc 就从 JS 里**推断**类型（checkJs 仍关：JS 库自己的写法不在这道门的判据里）。这是修，不是豁免：",
  "//14": "开之后基线从 86 掉到 76、两个文件清零，而不是多记一条债。",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "types": ["node"],
    "noEmit": true,
    "strict": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "allowJs": true,
    "allowImportingTsExtensions": true
  },
  "include": ["src/**/*.test.ts", "electron/**/*.test.ts", "evals/**/*.test.ts", "scripts/**/*.ts", "src/**/*.d.ts"]
}
